extends layout 

block content 

    h1= title 
    if song_list.length 
        ul 
            each song in song_list 
                li 
                    a(href=song.url) #{song.title} 
                    |   (#{song.author.name}) [#{song.channel_released} Release]
    else 
        p There are no songs.
