extends layout 

block content 
    h1= title 

    if author_list.length 
        ul 
            each author in author_list 
                li 
                    a(href=author.url) #{author.name} (#{author.date_of_birth_formatted})
                    |   featured (#{author.featured})
    else 
        p There are no authors.